# mtn2git

A shell script to help mirror a monotone repository to a remote git instance.

It will export all branches, so you better use a "limited" monotone database containing only the branches relative to the single project.
